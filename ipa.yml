---
- hosts: localhost
  gather_facts: false
  tasks:

  - name: ls
    shell: touch /runner/papka/test.yml
    register: out
    #failed_when: "out.rc not in [ 0, 127 ]"
  - name: print
    debug:
      var: out
